# VCS dependencies that cannot be hash-verified
# Install separately after hash-verified dependencies:
#   pip install --require-hashes -r requirements.txt
#   pip install --no-deps -r requirements-vcs.txt

git+https://github.com/312-dev/monarchmoney.git@bc77f4b  # custom header support + baseDate field + enable_category_rollover + get_savings_goals + API methods for Eclosion
